sqlite:
  dsn: migrate.db

plugins_preset: all
plugins:
  dbinfo:
    destination: internal/database/gen/dbinfo
  enums:
    destination: internal/database/gen/enums
  models:
    destination: internal/database/gen/models
  dberrors:
    destination: internal/database/gen/dberrors
  factory:
    destination: internal/database/gen/factory
